@model IEnumerable<Category>

@{
    ViewData["Title"] = "Shop";
}
<h1>Shop for the best in British Food!</h1>

<ul>
    @foreach (var category in Model)
    {
        <li>
            <a asp-action="ByCategory" asp-route-id="@category.CategoryId">@category.Name</a>
        </li>
    }
</ul>